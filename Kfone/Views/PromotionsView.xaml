<Page
    x:Class="Kfone.Views.PromotionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kfone.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel
            Grid.Column="1"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            >
            <Grid HorizontalAlignment="Right">
                <Button x:Name="addPromotionButton" Content="Add Promotion" Click="addPromotionButton_Click" Margin="{ThemeResource SmallLeftTopRightBottomMargin}" Background="{ThemeResource SystemAccentColor}"/>
                <Button x:Name="cancelPromotionButton" Content="Cancel" Click="cancelPromotionButton_Click" Margin="{ThemeResource SmallLeftTopRightBottomMargin}"/>
            </Grid>

            <Frame Name="Frame" Width="auto" Height="auto"></Frame>
        </StackPanel>
    </Grid>
</Page>
