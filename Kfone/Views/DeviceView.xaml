<Page
    x:Class="Kfone.Views.DeviceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kfone.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel
            Grid.Column="1"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            >
            <Grid HorizontalAlignment="Right">
                <Button x:Name="addDeviceButton" Content="Add Device" Click="addDeviceButton_Click" Margin="{ThemeResource SmallLeftTopRightBottomMargin}"/>
                <Button x:Name="cancelDeviceButton" Content="Cancel" Click="cancelDeviceButton_Click" Margin="{ThemeResource SmallLeftTopRightBottomMargin}"/>
            </Grid>
            
            <Frame Name="Frame" Width="auto" Height="auto"></Frame>
        </StackPanel>
    </Grid>
</Page>
